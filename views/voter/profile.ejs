<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Voter Profile | LINCSSA VOTES</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Font Awesome for icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
    />
    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <!-- AOS Animation library -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet" />
    <!-- Enhanced visibility styles -->
    <link href="/styles/enhanced-visibility.css" rel="stylesheet" />

    <style>
      :root {
        --primary: #5e60ce;
        --secondary: #4cc9f0;
        --success: #10b981;
        --warning: #f59e0b;
        --error: #ef4444;
        --gray-300: #e5e7eb;
        --gray-400: #d1d5db;
        --gray-500: #9ca3af;
        --text-primary: #ffffff;
        --text-secondary: rgba(255, 255, 255, 0.9);
        --text-muted: rgba(255, 255, 255, 0.7);
      }

      body {
        font-family: "Outfit", sans-serif;
        background-color: #0f172a;
        color: var(--text-primary);
        min-height: 100vh;
      }

      .sidebar {
        background-color: rgba(17, 24, 39, 0.9);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border-right: 1px solid rgba(255, 255, 255, 0.05);
        position: fixed;
        height: 100vh;
        z-index: 100;
        transition: all 0.3s;
      }

      .sidebar-header {
        padding: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      }

      .logo {
        font-size: 1.8rem;
        font-weight: 800;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      .sidebar-link {
        color: var(--gray-400);
        padding: 1rem 1.5rem;
        transition: all 0.3s;
        text-decoration: none;
        display: flex;
        align-items: center;
        border-left: 3px solid transparent;
      }

      .sidebar-link:hover {
        background: rgba(255, 255, 255, 0.05);
        color: white;
      }

      .sidebar-link.active {
        color: white;
        background: linear-gradient(90deg, rgba(94, 96, 206, 0.1), transparent);
        border-left: 3px solid var(--primary);
      }

      .sidebar-icon {
        margin-right: 1rem;
        font-size: 1.2rem;
        width: 20px;
        text-align: center;
      }

      .main-content {
        transition: all 0.3s;
        padding: 2rem;
      }

      .card {
        background-color: rgba(31, 41, 55, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
        border-radius: 0.75rem;
        transition: all 0.3s;
        margin-bottom: 1.5rem;
        color: var(--text-primary);
      }

      .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
      }

      .card-header {
        background-color: rgba(94, 96, 206, 0.15);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        padding: 1.5rem;
      }

      .profile-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 2rem;
      }

      .profile-avatar {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.05);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 4rem;
        color: var(--gray-300);
        margin-bottom: 1.5rem;
      }

      .profile-info {
        margin-bottom: 1.5rem;
      }

      .info-item {
        display: flex;
        margin-bottom: 1rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      }

      .info-item:last-child {
        border-bottom: none;
      }

      .info-icon {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: rgba(94, 96, 206, 0.2);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
        color: var(--secondary);
        margin-right: 1rem;
      }

      .info-label {
        color: var(--gray-300);
        font-size: 0.9rem;
        margin-bottom: 0.25rem;
      }

      .info-value {
        font-weight: 600;
        color: var(--text-primary);
      }

      .voting-activity {
        padding: 1.5rem;
      }

      .activity-item {
        display: flex;
        align-items: center;
        margin-bottom: 1.5rem;
        padding-bottom: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      }

      .activity-item:last-child {
        margin-bottom: 0;
        padding-bottom: 0;
        border-bottom: none;
      }

      .activity-icon {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
        margin-right: 1rem;
        flex-shrink: 0;
      }

      .activity-icon.vote {
        background: rgba(94, 96, 206, 0.1);
        color: var(--primary);
      }

      .activity-icon.login {
        background: rgba(76, 201, 240, 0.1);
        color: var(--secondary);
      }

      .footer {
        background-color: rgba(31, 41, 55, 0.7);
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        padding: 1.5rem 0;
        margin-top: 4rem;
        text-align: center;
      }

      /* Background effect */
      .bg-dots {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: radial-gradient(
          rgba(255, 255, 255, 0.15) 2px,
          transparent 2px
        );
        background-size: 30px 30px;
        z-index: -1;
        pointer-events: none;
      }

      .bg-glow {
        position: fixed;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(
          circle at center,
          rgba(94, 96, 206, 0.05) 0%,
          rgba(15, 23, 42, 0) 50%
        );
        z-index: -2;
        pointer-events: none;
      }

      .btn-primary {
        background: var(--primary);
        border-color: var(--primary);
      }

      .btn-primary:hover {
        background: #4e50ae;
        border-color: #4e50ae;
        transform: translateY(-2px);
      }

      .badge-level {
        background: rgba(76, 201, 240, 0.2);
        color: var(--secondary);
        padding: 0.4rem 0.8rem;
        border-radius: 2rem;
        font-size: 0.9rem;
      }

      /* Responsive */
      @media (max-width: 991.98px) {
        .sidebar {
          width: 250px;
          margin-left: -250px;
        }

        .sidebar.active {
          margin-left: 0;
        }

        .main-content {
          width: 100%;
          margin-left: 0;
        }

        .main-content.active {
          margin-left: 250px;
          width: calc(100% - 250px);
        }
      }
    </style>
  </head>
  <body>
    <!-- Background effects -->
    <div class="bg-dots"></div>
    <div class="bg-glow"></div>

    <div class="container-fluid">
      <div class="row">
        <!-- Sidebar -->
        <div class="col-lg-3 col-xl-2 p-0">
          <nav class="sidebar">
            <div
              class="sidebar-header d-flex justify-content-between align-items-center"
            >
              <div class="logo">LINCSSA</div>
              <button class="btn btn-link d-lg-none" id="sidebarToggle">
                <i class="fas fa-bars text-white"></i>
              </button>
            </div>

            <div class="sidebar-menu mt-4">
              <a href="/dashboard" class="sidebar-link">
                <i class="fas fa-th-large sidebar-icon"></i> Dashboard
              </a>
              <a href="/vote" class="sidebar-link">
                <i class="fas fa-vote-yea sidebar-icon"></i> Vote
              </a>
              <a href="/results" class="sidebar-link">
                <i class="fas fa-chart-bar sidebar-icon"></i> Results
              </a>
              <a href="/profile" class="sidebar-link active">
                <i class="fas fa-user sidebar-icon"></i> Profile
              </a>
              <div class="mt-auto">
                <a href="/logout" class="sidebar-link text-danger">
                  <i class="fas fa-sign-out-alt sidebar-icon"></i> Logout
                </a>
              </div>
            </div>
          </nav>
        </div>

        <!-- Main content -->
        <div class="col-lg-9 col-xl-10 main-content">
          <!-- Status messages -->
          <% if (messages && (messages.error || messages.success ||
          messages.info)) { %>
          <div class="mb-4">
            <% if (messages.error) { %>
            <div class="alert alert-danger" data-aos="fade-up">
              <i class="fas fa-exclamation-circle me-2"></i>
              <%= messages.error %>
            </div>
            <% } %> <% if (messages.success) { %>
            <div class="alert alert-success" data-aos="fade-up">
              <i class="fas fa-check-circle me-2"></i>
              <%= messages.success %>
            </div>
            <% } %> <% if (messages.info) { %>
            <div class="alert alert-info" data-aos="fade-up">
              <i class="fas fa-info-circle me-2"></i>
              <%= messages.info %>
            </div>
            <% } %>
          </div>
          <% } %>

          <!-- Profile Header -->
          <div class="profile-header" data-aos="fade-up">
            <div>
              <h1 class="mb-2">Your Profile</h1>
              <p class="text-muted">View and manage your voter information</p>
            </div>
          </div>

          <div class="row">
            <!-- Personal Information Card -->
            <div class="col-md-8" data-aos="fade-up">
              <div class="card">
                <div class="card-header">
                  <h2 class="mb-0">Personal Information</h2>
                </div>
                <div class="card-body">
                  <div class="text-center mb-4">
                    <div class="profile-avatar mx-auto">
                      <i class="fas fa-user"></i>
                    </div>
                    <h3><%= voter?.fullName || 'John Doe' %></h3>
                    <div class="badge-level">
                      <%= voter?.level || '100' %> Level
                    </div>
                  </div>

                  <div class="profile-info">
                    <div class="info-item">
                      <div class="info-icon">
                        <i class="fas fa-id-card"></i>
                      </div>
                      <div>
                        <div class="info-label">Matric Number</div>
                        <div class="info-value">
                          <%= voter?.matricNumber || 'XXXX/XXXXX' %>
                        </div>
                      </div>
                    </div>

                    <div class="info-item">
                      <div class="info-icon">
                        <i class="fas fa-university"></i>
                      </div>
                      <div>
                        <div class="info-label">Department</div>
                        <div class="info-value">
                          <%= voter?.department || 'Linguistics and
                          Communication Studies' %>
                        </div>
                      </div>
                    </div>

                    <div class="info-item">
                      <div class="info-icon">
                        <i class="fas fa-phone"></i>
                      </div>
                      <div>
                        <div class="info-label">Phone Number</div>
                        <div class="info-value">
                          <%= voter?.phoneNumber || '080XXXXXXXX' %>
                        </div>
                      </div>
                    </div>

                    <div class="info-item">
                      <div class="info-icon">
                        <i class="fas fa-calendar"></i>
                      </div>
                      <div>
                        <div class="info-label">Registered On</div>
                        <div class="info-value">
                          <% if (voter && voter.created_at) { %> <%= new
                          Date(voter.created_at).toLocaleDateString('en-US', {
                          year: 'numeric', month: 'long', day: 'numeric' }) %>
                          <% } else { %> July 3, 2025 <% } %>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <!-- Voting Status Card -->
            <div class="col-md-4" data-aos="fade-up" data-aos-delay="100">
              <div class="card">
                <div class="card-header">
                  <h2 class="mb-0">Voting Status</h2>
                </div>
                <div class="card-body text-center">
                  <div class="mb-4">
                    <i class="fas fa-vote-yea fa-4x text-primary mb-3"></i>
                    <h3>
                      <%= voter?.votedPositions ? voter.votedPositions.length :
                      0 %>
                      <small class="text-muted"
                        >/ <%= positions ? positions.length : 0 %></small
                      >
                    </h3>
                    <p>Positions Voted</p>
                  </div>

                  <div class="progress mb-4" style="height: 10px">
                    <% let progressValue = 0; if (voter?.votedPositions &&
                    positions) { progressValue =
                    Math.round((voter.votedPositions.length / positions.length)
                    * 100); } %>
                    <div
                      class="progress-bar"
                      role="progressbar"
                      data-width="<%= progressValue %>"
                      style="width: 0%"
                      aria-valuenow="<%= progressValue %>"
                      aria-valuemin="0"
                      aria-valuemax="100"
                    ></div>
                  </div>

                  <div>
                    <% if (voter?.votedPositions && positions &&
                    voter.votedPositions.length === positions.length) { %>
                    <div class="alert alert-success">
                      <i class="fas fa-check-circle me-2"></i>
                      You've voted for all positions
                    </div>
                    <% } else { %>
                    <a href="/vote" class="btn btn-primary w-100">
                      <i class="fas fa-vote-yea me-2"></i> Continue Voting
                    </a>
                    <% } %>
                  </div>
                </div>
              </div>

              <!-- Voter Verification Card -->
              <div class="card" data-aos="fade-up" data-aos-delay="150">
                <div class="card-header">
                  <h2 class="mb-0">Verification Status</h2>
                </div>
                <div class="card-body text-center">
                  <div class="mb-3">
                    <i class="fas fa-check-circle fa-3x text-success mb-3"></i>
                    <h4>Verified Voter</h4>
                  </div>
                  <p class="text-muted mb-0">
                    Your account is verified and eligible to vote in the LINCSSA
                    elections.
                  </p>
                </div>
              </div>
            </div>
          </div>

          <!-- Recent Activity -->
          <div class="card mt-4" data-aos="fade-up" data-aos-delay="200">
            <div class="card-header">
              <h2 class="mb-0">Recent Activity</h2>
            </div>
            <div class="voting-activity">
              <% if (voter && voter.votedPositions &&
              voter.votedPositions.length > 0) { %> <% for (let i = 0; i <
              Math.min(voter.votedPositions.length, 5); i++) { %>
              <div class="activity-item">
                <div class="activity-icon vote">
                  <i class="fas fa-vote-yea"></i>
                </div>
                <div class="flex-grow-1">
                  <h5 class="mb-1">Voted for a position</h5>
                  <p class="text-muted mb-0">
                    You cast your vote for a candidate.
                  </p>
                </div>
                <div class="activity-time">
                  <% const date = new Date(); date.setHours(date.getHours() - i
                  * 2); %> <%= date.toLocaleString('en-US', { month: 'short',
                  day: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
                </div>
              </div>
              <% } %> <% } else { %>
              <div class="activity-item">
                <div class="activity-icon login">
                  <i class="fas fa-sign-in-alt"></i>
                </div>
                <div class="flex-grow-1">
                  <h5 class="mb-1">Account Login</h5>
                  <p class="text-muted mb-0">You logged into your account.</p>
                </div>
                <div class="activity-time">
                  <%= new Date().toLocaleString('en-US', { month: 'short', day:
                  'numeric', hour: '2-digit', minute: '2-digit' }) %>
                </div>
              </div>
              <div class="alert alert-info mt-3">
                <i class="fas fa-info-circle me-2"></i>
                You haven't voted for any positions yet. Visit the
                <a href="/vote" class="alert-link">Voting page</a> to
                participate.
              </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
      <div class="container">
        <p class="mb-1">
          © 2025 LINCSSA VOTES - Osun State University, Ikire Campus
          Departmental Election
        </p>
        <p class="mb-0 text-muted small">Your voice matters. Make it count.</p>
      </div>
    </footer>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <!-- AOS Animation -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <!-- Progress bar initialization -->
    <script src="/script/progress-bars.js"></script>

    <script>
      document.addEventListener("DOMContentLoaded", function () {
        // Initialize AOS animations
        AOS.init({
          duration: 800,
          easing: "ease-out",
          once: true,
        });

        // Sidebar toggle for mobile
        const sidebarToggle = document.getElementById("sidebarToggle");
        const sidebar = document.querySelector(".sidebar");
        const mainContent = document.querySelector(".main-content");

        if (sidebarToggle) {
          sidebarToggle.addEventListener("click", function () {
            sidebar.classList.toggle("active");
            mainContent.classList.toggle("active");
          });
        }
      });
    </script>
  </body>
</html>
